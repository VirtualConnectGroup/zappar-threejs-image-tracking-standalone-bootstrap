import { Localisations, SnapshotOptions } from './spec';
import ElementFactory from './elementFactory';
export default class SnapshotManager extends ElementFactory {
    private containerPreview;
    private containerButtons;
    private instructionsText;
    static _dialogOpen: boolean;
    captureimage(captureParameters: SnapshotOptions, localisation: Localisations): void;
    private _displayMessage;
    static _dataURItoBlob(dataURI: string): Promise<Blob>;
    shareVideo(captureParameters: SnapshotOptions, localisation: Localisations): void;
    private static _prepareShare;
    destroy(): void;
    private static _getFileName;
    static _dataURItoFileBlob(dataURI: string, fileType: '.jpeg' | '.mp4'): Promise<File>;
}
